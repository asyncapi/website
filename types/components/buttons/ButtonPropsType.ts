export enum ButtonSize {
  SMALL = 'small',
  DEFAULT = 'default'
}

export enum ButtonType {
  SUBMIT = 'submit',
  RESET = 'reset',
  BUTTON = 'button'
}

export enum ButtonIconPosition {
  LEFT = 'left',
  RIGHT = 'right'
}
